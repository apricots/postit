<%= render 'shared/content_title', title: 'New Category' %>

<%= render 'shared/check_error', obj: @category %>

<div class ='well'>

    <%= form_for @category do |f| %>
      <div class = 'control-group'>
      <%= f.label :name %>
      <%= f.text_field :name %>
  </div>
   <%= f.submit "New Category", class: 'btn btn-custom' %>
    <% end %>
  </div>
</div>